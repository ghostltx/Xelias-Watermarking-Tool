{
  "name": "xelias-watermarking-tool",
  "version": "1.1.0",
  "private": true,
  "description": "Electron desktop wrapper for the Xelias Watermarking Tool",
  "main": "electron/main.cjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check electron/main.cjs && node --check electron/preload.cjs && npm test"
  },
  "devDependencies": {
    "electron": "^42.4.1"
  }
}
